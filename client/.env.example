# Local Docker dev: point at Traefik so the client reaches the API on the same origin.
# If you run the Go API directly on the host instead, set this to that host URL.
NEXT_PUBLIC_API_URL=http://localhost
NEXT_PUBLIC_CLOUDFRONT_URL=https://replace-me.cloudfront.net
