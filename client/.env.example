# Local dev: point directly at the Go server.
# Staging/production: point at the public Tailscale Funnel URL for the API.
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_CLOUDFRONT_URL=https://replace-me.cloudfront.net
